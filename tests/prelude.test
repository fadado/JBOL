#########################################################################
# Prelude
#
# Tests are groups of three lines: program, input, expected output.
# Blank lines and lines starting with # are ignored.
#########################################################################

#include "prelude"; reduce .[] as $n (0; .+$n) == fold(.[0]+.[1]; 0; .[])
#[1,2,3,4]
#true

include "prelude"; [limit(6; tabulate(pow(.;2)))]
null
[0,1,4,9,16,25]


include "prelude"; failure(empty)
null
true

include "prelude"; failure(range(0))
null
true

include "prelude"; failure(true)
null
false

include "prelude"; failure(false)
null
false

include "prelude"; failure(null)
null
false

include "prelude"; failure(range(8))
null
false

include "prelude"; failure(1,2,3)
null
false

include "prelude"; failure(0)
null
false

include "prelude"; failure(infinite)
null
false

include "prelude"; failure(nan)
null
false

include "prelude"; every(empty)
null
true

include "prelude"; some(empty)
null
false

# vim:ai:sw=2:ts=2:et:syntax=jq
